import { IconButton } from "../../atoms/buttons/IconButton";

export const Header = () => {
    return (
        <header className="flex f-center f-space-between">
            <h5>Robert's To-Do App</h5>
            <IconButton></IconButton>
        </header>
    );
}